{
  "name": "pe-dashboard",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "npm run dev -w server & npm run dev -w web & wait",
    "test": "npm test -w server"
  }
}
